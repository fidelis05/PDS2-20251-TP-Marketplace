#ifndef PEDIDO_HPP
#define PEDIDO_HPP

#include "Produto.hpp"
#include "Usuario.hpp"
#include <iostream>

struct Pedido
{
  int = ID std::string = created_at std::string = codigo_rastreio

  Pedido(Usuario x, Carrinho y);
};

#endif
